<h1>Hello Rails</h1>
<% @articles.each do |article| %>
  <article>
    <h2>
      <%= link_to article.title, article %>
    </h2>
    <%= article.body %>
  </article>
<% end %>

<%= link_to "New Article", new_article_path %>
